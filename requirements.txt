claude-agent-sdk==0.2.124
python-telegram-bot[job-queue]==22.8
httpx==0.28.1
# Расписание коуча переехало в мозг и читается кодом — значит нужен настоящий
# разбор YAML, а не регулярка: опечатка должна быть ошибкой, а не тихим молчанием.
pyyaml>=6,<7
